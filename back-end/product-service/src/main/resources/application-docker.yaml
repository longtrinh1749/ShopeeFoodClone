spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://database:3306/product_service?serverTimezone=UTC
    username: shopeefood
    password: shopeefood
    driver-class-name: org.mariadb.jdbc.Driver

server:
  port: 8500